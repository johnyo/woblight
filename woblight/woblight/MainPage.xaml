<Page
    x:Class="woblight.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:woblight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        
        <!-- Wob background -->
        <Rectangle Name="myBackground" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="Blue"/>

        <!-- Optional Wob Light image overlay-->
        <Image Name="myWoblightOverlay" Source="woblight.png" Visibility="Collapsed" />
       
        <!-- Optional Wob Light text overlay -->
        <TextBlock Name="myTextOverlay" Text="John" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="550" Foreground="Red" FontWeight="Bold" />

        <!-- Invisible button overlawy to allow wobbing -->
        <Button Name="myButton" PointerMoved="Button_Click"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Opacity="0.0" Background="#B3C98888" />   
        
    </Grid>

</Page>
